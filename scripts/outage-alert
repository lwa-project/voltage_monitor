#!/bin/sh

### BEGIN INIT INFO
# Provides:        outage-alert
# Required-Start:  voltage-monitor
# Required-Stop:   none
# Default-Start:   2 3 4 5
# Default-Stop:    1
# Short-Description: Start the outage alert
### END INIT INFO

. /lib/lsb/init-functions

DAEMON=/lwa/LineMonitoring/scripts/sendPowerEmail.py
PIDFILE=/var/run/outageAlert.pid

#test -x $DAEMON || exit 5

ALERT_OPTS=""

LOCKFILE=/var/lock/outageAlert

lock_can() {
	if [ -x /usr/bin/lockfile-create ]; then
		lockfile-create $LOCKFILE
		lockfile-touch $LOCKFILE &
		LOCKTOUCHPID="$!"
	fi
}

unlock_can() {
	if [ -x /usr/bin/lockfile-create ] ; then
		kill $LOCKTOUCHPID
		lockfile-remove $LOCKFILE
	fi
}

case $1 in
	start)
		log_daemon_msg "Starting power outage monitor" "sendPowerEmail.py"
		lock_can
  		start-stop-daemon --start --quiet --oknodo --pidfile $PIDFILE --startas $DAEMON -- -i $PIDFILE $ALERT_OPTS
		status=$?
		unlock_can
		log_end_msg $status
  		;;
	stop)
		log_daemon_msg "Stopping power outage monitor" "sendPowerEmail.py"
  		start-stop-daemon --stop --quiet --oknodo --pidfile $PIDFILE
		log_end_msg $?
		rm -f $PIDFILE
  		;;
	restart|force-reload)
		$0 stop && sleep 2 && $0 start
  		;;
	try-restart)
		if $0 status >/dev/null; then
			$0 restart
		else
			exit 0
		fi
		;;
	reload)
		exit 3
		;;
	status)
		status_of_proc -p $PIDFILE $DAEMON "lightning monitor"
		;;
	*)
		echo "Usage: $0 {start|stop|restart|try-restart|force-reload|status}"
		exit 2
		;;
esac
